// .js sample files under src/samples/ are handled by raw-loader
// (configured in next.config.ts with include: src/samples).
// The imports in sampleRegistry.ts use @ts-expect-error since
// TypeScript can't know about the webpack loader pipeline.
